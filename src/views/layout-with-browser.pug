extends layout

append content
    div(id="embedded-browser-container")
        form(id="embedded-browser-controls", method="POST")
            input(type="url", name="location")
            button GO
        iframe(id="embedded-browser")