extends layout
    
block content    
    h1 Paths
    a(href="/paths/" + newId) Create
    ul
        each path in paths
            li
                a(href="/paths/" + path.id) #{path.name} - #{path.modified.toDateString()}
    